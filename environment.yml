name: Stock_Price_Prediction
dependencies:
  - python=3.10.0
  - pandas
  - numpy
  - scikit-learn
  - jupyter
  - matplotlib
  - pytest
  - python-dateutil
  - pip
  - pip:
      - valkey
      - "valkey[hiredis]"
      - dvc[azure]
      - azure-cli
